name: Echo on Src1 Directory Change

on:
  push:
    paths:
      - 'Src1/**'

jobs:
  echo-message:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Echo Message
        run: echo "Changes detected in Src1 directory"

